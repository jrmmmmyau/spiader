slam_toolbox:
  ros__parameters:
    base_frame: base_link
    odom_frame: odom
    map_frame: map
    use_sim_time: false
    scan_topic: /scan
    subscriber_queue_size: 50
    map_update_interval: 2.0
